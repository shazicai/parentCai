<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
              http://dubbo.apache.org/schema/dubbo
              http://dubbo.apache.org/schema/dubbo/dubbo.xsd">
    <!--  服务提供方   -->
    <dubbo:application name="${application.name}" default="true"/>
    <!--  此处使用zookeeper注册中心暴露服务地址  -->
    <dubbo:registry id="register" address="${register.address}" file="${cache}"/>
    <!--  协议和端口暴露  -->
    <dubbo:protocol name="${name}" port="20880" />

    <!-- user服务暴露 -->
    <dubbo:service registry="register" interface="com.service.springService.user.service.UserService"
                   ref="userService" retries="0" timeout="200000" version="1.0.0"/>
</beans>